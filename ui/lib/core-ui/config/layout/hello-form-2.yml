type: record
resource: Layout
namespace: ui
properties:
  name: HelloForm2
  layoutComponent:
    $extend:
      $namespace: ui
      $resource: Layout
      $match:
        name: HelloForm
      $ref: $.properties.layoutComponent
    $override:
      $select: $
      $merge:
        params:
          - name: world
            label: World
            type: STRING
            required: true
            length: 255
            default: World
