type: record
resource: Layout
namespace: ui
properties:
  name: CrudFunction
  layoutComponent:
    $import: Layout/crud
    namespace: logic
    resource: Function
    debug: false
    $override:
      $select: $.properties.defs.form.children.children
      $set:
        component: Box
        label: Properties
        children:
          - $import: Input/TextField
          - $import: Input/TextField
          - $import: Input/FunctionExecField
            algorithm: field1.algorithm
          - $import: Input/TextField
