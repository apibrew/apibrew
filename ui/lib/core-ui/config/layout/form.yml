type: record
resource: Layout
namespace: ui
properties:
  name: Form
  layoutComponent:
    component: CoreUI/Form
    params:
      $syntax:
        $properties:
          isNew: bool
          viewOnly: bool
