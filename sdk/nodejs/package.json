{
  "name": "@apibrew/client",
  "private": false,
  "version": "1.0.94",
  "description": "Client library for API Brew client",
  "scripts": {
    "generate-model": "apbr generate --namespace=logic --platform=typescript --path=src/model/logic;apbr generate --namespace=system --platform=typescript --path=src/model/system",
    "prepublish": "tsc",
    "deploy": "tsc; npm version patch; npm publish --access public"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "author": "Taleh Ibrahimli",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^18.15.11",
    "typescript": "^5.0.4"
  },
  "dependencies": {
    "axios": "^1.4.0"
  }
}
