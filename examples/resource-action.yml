type: record
resource: ResourceAction
namespace: system
properties:
  resource:
    namespace:
      name: default
    name: Account
  name: invalidate-password
  input:
    - name: tp
      type: STRING
  output:
    type: OBJECT
