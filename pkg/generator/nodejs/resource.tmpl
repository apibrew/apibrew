import {Entity} from "data-handler-client";

{{ range .params.Resources}}
export interface {{ToCamel .Name}} extends Entity {

    {{ range .Properties }}{{ToLowerCamel .Name}}: {{PropNodejsType .}};
    {{end}}
}
{{end}}
